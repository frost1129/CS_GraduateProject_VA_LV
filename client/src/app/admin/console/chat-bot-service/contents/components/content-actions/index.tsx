import { ContentActionsProps } from "@/lib/types/component";

const ContentActions = (props: ContentActionsProps) => {
    const { value } = props;

  return (
    <div>ContentActions</div>
  )
}

export default ContentActions